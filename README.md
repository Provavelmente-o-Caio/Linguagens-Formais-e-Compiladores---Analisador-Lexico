# INE5426 - Construção de Compiladores
O seguinte trabalho tem como objetivo:
- Construção de um analisador léxico para uma linguagem (AL);
- Construção de um analisador sintático para uma linguagem (AS);
- Construção de um analisador semântico para uma linguagem (ASem);
- Construção de um gerador de código intermediário (GCI);

# Autores
- Caio Ferreira Cardoso (22200352)
- Felipe Thaddeu Gassen (23100481)

# Organização

O código fonte pode ser encontrado em `src/`, a gramática fatorada e aplicações dela podem ser encontrados em `tests/`

# Tecnologias
- Python

As dependencias estão listadas em `requirements.txt`

Para rodar o projeto, basta executar `make` na raiz deste.